<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.dromara.third.mapper.ThirdProviderMapper">
    <select id="selectByProviderCode" resultType="org.dromara.third.domain.row.ThirdProviderRow">
        select provider_id, provider_code, provider_name, base_url, status, timeout_connect_ms,
               timeout_read_ms, rate_limit, concurrency_limit, shared_headers_json, remark, version
        from third_provider where provider_code = #{providerCode} and del_flag = '0'
    </select>
</mapper>
